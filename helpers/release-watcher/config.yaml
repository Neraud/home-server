logger:
  type: stdout
  level: INFO

#logger:
#  type: file
#  level: DEBUG
#  path: /data/tmp/release_watcher.log

core:
  threads: 5
  runMode: once

sources:
  - type: file
    path: tmp/watchers.yaml

outputs:
  - type: yaml_file
    path: tmp/out.yaml
    displayUpToDate: yes
