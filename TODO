
* Add CPU/mem limits on all containers

* Dashboard : without trailing /

* TT-RSS : inject config.php

* HomeAssistant : 
    * passthrough USB
    * Depends MQTT ?

* Node-RED : automatically install custom nodes

* RoomAssistant :
    * BLE
    * Depends MQTT ?

* Add NAS mount (maybe in a PV ?)

* Airsonic : configure the /music volume
