# {{ ansible_managed }}

[Unit]
Description=Backup apps data with restic

[Service]
Type=oneshot
Nice=10
ExecStart={{ restic_backup_scripts_dir }}/restic_backup_apps.sh
# $HOME or $XDG_CACHE_HOME must be set for restic to find /root/.cache/restic/
Environment="HOME=/root"
