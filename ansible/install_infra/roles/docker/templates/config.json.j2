{
    "auths": {
{% for auth in docker_auth_repositories %}
        "{{ auth.url }}": {
            "auth": "{{ (auth.username +':'+auth. password) | b64encode }}"
        }{% if not loop.last %},{% endif %}

{% endfor %}
    }
}
