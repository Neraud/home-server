---
- name: Restart docker daemon
  ansible.builtin.systemd:
    name: docker
    state: restarted
    daemon_reload: yes

- name: Restart avahi
  ansible.builtin.systemd:
    name: avahi-daemon
    state: restarted
