---

kubernetes_user:
  name: "{{ standard_user.name }}"
  home: "{{ standard_user.home }}"

kubernetes_cni: calico
kubernetes_pod_network_cidr: 10.244.0.0/16

# Flannel uses the interface associated with the default route by default.
#kubernetes_flannel_interface:

kubernetes_k8s_bridge_name: k8s-bridge
kubernetes_k8s_bridge_namespace: kube-system
kubernetes_k8s_bridge_cidr: 10.10.0.0/16
