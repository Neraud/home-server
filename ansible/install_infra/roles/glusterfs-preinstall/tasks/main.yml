---

# With Debian Buster, only v5 was present in the official repo, so we needed gluster repos
# With Debian Bullseye, v9 is present in the official repo, which is recent enough
# (we might want to enable custom repo again in the future, but for now it's not needed)

#- name: Add Gluster repo key
#  apt_key:
#    url: https://download.gluster.org/pub/gluster/glusterfs/9/rsa.pub
#    state: present
#
#- name: Add Gluster repo
#  apt_repository:
#    filename: "gluster"
#    repo: "deb https://download.gluster.org/pub/gluster/glusterfs/9/9.2/Debian/{{ ansible_lsb.codename }}/amd64/apt {{ ansible_lsb.codename }} main"
#    state: present
