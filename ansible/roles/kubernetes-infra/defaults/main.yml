---

web_base_domain: domain.tld

kubernetes_user:
  name: "{{ standard_user.name }}"
  home: "{{ standard_user.home }}"

#kubernetes_nginx_ingress_mqtt_target: "mqttservice:mqttport"

kubernetes_local_lvm_storage_class_name: local-lvm
kubernetes_nas_nfs_storage_class_name: nas-nfs

#kubernetes_nfs_storage:
#  nfs-whatever: 
#    nfs_server: 
#    nfs_path: 
#    reclaim_policy: Retain
#    labels:
#      detail: whatever

kubernetes_extra_namespaces:
  - ingress-nginx
  - monitoring
  - logging

kubernetes_cluster_ca_namespaces:
  - default
  - ingress-nginx
  - monitoring
  - logging

kubernetes_ingress_tls_secret_namespaces:
  - default
  - monitoring
  - logging
