---

- name: Set mount path
  set_fact:
    grafana_mount_path: "{{ kubernetes_local_storage['grafana-data']['mount_path'] }}"

- name: Chown folder to Grafana user
  file:
    path: "{{ grafana_mount_path }}"
    state: directory
    owner: 65534
    group: 65534
