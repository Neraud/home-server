---

- name: Install python dependencies
  apt:
    pkg:
    - python-pip
    - python-setuptools
    - python-docker
    state: present

- name: Install pip dependencies
  pip:
    name:
    - PyYAML
    - openshift
    state: present
