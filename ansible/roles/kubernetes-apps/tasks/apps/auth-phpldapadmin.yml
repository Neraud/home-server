---

- name: List config files
  set_fact:
    phpldapadmin_config_files: "{{ phpldapadmin_config_files | default([]) + [ item ] }}"
  with_fileglob:
    - "{{ kubernetes_app_root_path }}/auth/phpldapadmin/config/*.yaml*"

- name: Deploy phpLDAPadmin
  k8s:
    definition: "{{ lookup(item | regex_search('.j2$') | ternary('template','file'), item) }}"
    state: present
  with_fileglob:
    - "{{ kubernetes_app_root_path }}/auth/phpldapadmin/deploy/*.yaml*"
  become: yes
  become_user: "{{ kubernetes_user.name }}"
