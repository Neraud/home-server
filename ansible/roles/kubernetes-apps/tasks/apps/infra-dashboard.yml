---

- name: Deploy Kubernetes dashboard
  k8s:
    definition: "{{ lookup(item | regex_search('.j2$') | ternary('template','file'), item) }}"
    state: present
  with_fileglob:
    - "{{ kubernetes_app_root_path }}/infra/dashboard/deploy/*.yaml*"
  become: yes
  become_user: "{{ kubernetes_user.name }}"
