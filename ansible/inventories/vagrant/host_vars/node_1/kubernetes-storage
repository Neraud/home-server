---

kubernetes_local_storage:
  registry-data:
    capacity: 10G
    reclaim_policy: Retain
    labels:
      app: registry
      app-component: registry
  openldap-data:
    capacity: 256M
    reclaim_policy: Retain
    labels:
      app: openldap
      app-component: openldap
      storage: data
  openldap-config:
    capacity: 16M
    reclaim_policy: Retain
    labels:
      app: openldap
      app-component: openldap
      storage: config
  lemonldap-data:
    capacity: 32M
    reclaim_policy: Retain
    labels:
      app: lemonldap
      app-component: lemonldap
      storage: data
  ttrss-pgsql: 
    capacity: 1G
    reclaim_policy: Retain
    labels:
      app: ttrss
      app-component: pgsql
  airsonic-data:
    capacity: 256M
    reclaim_policy: Retain
    labels:
      app: airsonic
      app-component: airsonic
      storage: data
  airsonic-playlists:
    capacity: 256M
    reclaim_policy: Retain
    labels:
      app: airsonic
      app-component: airsonic
      storage: playlists
  airsonic-podcasts:
    capacity: 1G
    reclaim_policy: Retain
    labels:
      app: airsonic
      app-component: airsonic
      storage: podcasts
  plex-data:
    capacity: 2G
    reclaim_policy: Retain
    labels:
      app: plex
      app-component: plex
  gitlab-config:
    capacity: 8M
    filesystem: ext4
    mount_path: /opt/kubernetes_storage/gitlab-config
    reclaim_policy: Retain
    labels:
      app: gitlab
      app-component: gitlab
      storage: config
  gitlab-data:
    capacity: 2G
    reclaim_policy: Retain
    labels:
      app: gitlab
      app-component: gitlab
      storage: data
  unifi-config:
    capacity: 1G
    reclaim_policy: Retain
    labels:
      app: unifi
      app-component: unifi
  prometheus-k8s:
    capacity: 10G
    reclaim_policy: Retain
    labels:
      app: prometheus-k8s
      app-component: prometheus
  grafana-data:
    capacity: 1G
    reclaim_policy: Retain
    filesystem: ext4
    mount_path: /opt/kubernetes_storage/grafana-data
    labels:
      app: grafana
      app-component: grafana
  elasticsearch-data:
    capacity: 10G
    reclaim_policy: Retain
    labels:
      app: elasticsearch
      app-component: elasticsearch
  homeassistant-mysql:
    capacity: 2G
    reclaim_policy: Retain
    labels:
      app: homeassistant
      app-component: mysql
