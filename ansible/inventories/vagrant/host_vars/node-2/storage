---

local_lvm_storage:
  plex-data:
    capacity: 2G
    reclaim_policy: Retain
    labels:
      app: plex
      app-component: plex
  gitlab-config:
    capacity: 8M
    filesystem: ext4
    mount_path: /data/volumes/gitlab-config
    reclaim_policy: Retain
    labels:
      app: gitlab
      app-component: gitlab
      storage: config
  gitlab-data:
    capacity: 2G
    reclaim_policy: Retain
    labels:
      app: gitlab
      app-component: gitlab
      storage: data
