---

kubernetes_local_storage:
  homeassistant-config:
    capacity: 1G
    filesystem: ext4
    mount_path: /opt/kubernetes_storage/homeassistant-config
    reclaim_policy: Retain
    labels:
      app: homeassistant
      app-component: homeassistant
  homeassistant-mysql:
    capacity: 2G
    reclaim_policy: Retain
    labels:
      app: homeassistant
      app-component: mysql
  nodered-frontend:
    capacity: 1G
    filesystem: ext4
    mount_path: /opt/kubernetes_storage/nodered-frontend
    reclaim_policy: Retain
    labels:
      app: nodered
      tier: frontend
  mosquitto-backend:
    capacity: 16M
    filesystem: ext4
    mount_path: /opt/kubernetes_storage/mosquitto-backend
    reclaim_policy: Retain
    labels:
      app: mosquitto
      tier: backend
  mosquitto-data:
    capacity: 128M
    filesystem: ext4
    mount_path: /opt/kubernetes_storage/mosquitto-data
    reclaim_policy: Retain
    labels:
      app: mosquitto
      tier: data
