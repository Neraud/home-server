---

glusterfs_volumes:
  registry-data:
    replicas: 2
    capacity: 10G
    arbiterCapacity: 256M
    nodes: [node-1, node-2]
    arbiters: [master]
    reclaim_policy: Retain
    labels:
      app: registry
      app-component: registry
  zonemta-mongo-data:
    replicas: 2
    capacity: 512M
    arbiterCapacity: 32M
    nodes: [node-1, node-2]
    arbiters: [master]
    reclaim_policy: Retain
    labels:
      app: zonemta
      app-component: mongo
  zonemta-redis-data:
    replicas: 2
    capacity: 32M
    arbiterCapacity: 4M
    nodes: [node-1, node-2]
    arbiters: [master]
    reclaim_policy: Retain
    labels:
      app: zonemta
      app-component: redis
