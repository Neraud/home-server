---

metallb_memberlist_secret: abcdefghijklmnopqrstuvwxyz
metallb_ip_pools:
- name: pool1
  addresses: 
  - 192.168.100.20-192.168.100.29

kubernetes_ingress_lemonLdap_handler_port: 30081
kubernetes_ingress_openldap_port: 30636
kubernetes_ingress_mqtt_port: 31883
# To avoid issues with having the same NodePort used for both protocols, we use different ports (see https://github.com/kubernetes/kubernetes/issues/85020)
kubernetes_pihole_tcp_port: 30053
kubernetes_pihole_udp_port: 30054

kubernetes_local_lvm_storage_class_name: local-lvm
kubernetes_glusterfs_storage_class_name: glusterfs

kubernetes_k8s_bridge_name: k8s-bridge
kubernetes_k8s_bridge_namespace: kube-system
k8s_bridge_fullname: "{{ kubernetes_k8s_bridge_namespace }}/{{ kubernetes_k8s_bridge_name }}"
