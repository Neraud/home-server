apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: {{ grafana.namespace }}
  labels:
    app.kubernetes.io/name: grafana
    prometheus-target: grafana
spec:
  ports:
  - name: http
    port: 3000
    targetPort: http
  selector:
    app.kubernetes.io/name: grafana
