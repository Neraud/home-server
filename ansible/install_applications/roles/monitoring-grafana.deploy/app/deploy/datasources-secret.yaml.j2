apiVersion: v1
kind: Secret
metadata:
  name: grafana-datasources
  namespace: monitoring
  labels:
    app: grafana
    app-component: grafana
type: Opaque
data:
  prometheus.yaml: {{ lookup('file', role_path + '/app/config/datasources/prometheus.yaml') | b64encode }}
