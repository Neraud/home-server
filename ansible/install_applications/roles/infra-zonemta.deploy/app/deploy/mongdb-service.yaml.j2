apiVersion: v1
kind: Service
metadata:
  name: zonemta-mongo
  namespace: "{{ zonemta.namespace }}"
  labels:
    app: zonemta
spec:
  ports:
  - port: 27017
    name: mongo
  selector:
    app: zonemta
    app-component: mongo
