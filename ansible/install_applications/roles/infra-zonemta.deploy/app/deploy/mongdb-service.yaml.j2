apiVersion: v1
kind: Service
metadata:
  name: zonemta-mongo
  namespace: "{{ zonemta.namespace }}"
  labels:
    app.kubernetes.io/name: zonemta
spec:
  ports:
  - port: 27017
    name: mongo
  selector:
    app.kubernetes.io/name: zonemta
    app.kubernetes.io/component: mongo
