apiVersion: v1
kind: Service
metadata:
  name: zonemta-redis
  namespace: "{{ zonemta.namespace }}"
  labels:
    app.kubernetes.io/name: zonemta
spec:
  ports:
  - port: 6379
    name: redis
  selector:
    app.kubernetes.io/name: zonemta
    app.kubernetes.io/component: redis
