---

web_base_domain: domain.tld

kubernetes_user:
  name: "{{ standard_user.name }}"
  home: "{{ standard_user.home }}"

zonemta_feeder_authentication_username: smtp
zonemta_feeder_authentication_password: clear_password

sickchill_enabled: true

sickchill_context_root: /
sickchill_api_key: "abcdefghijklmnopqrstuvwxyz123456"
sickchill_smtp_host: "{{ zonemta_smtp_servername }}"
sickchill_smtp_port: 587
sickchill_smtp_tls: True
sickchill_smtp_user: "{{ zonemta_feeder_authentication_username }}"
sickchill_smtp_password: "{{ zonemta_feeder_authentication_password }}"
sickchill_smtp_from: "noreply@{{ web_base_domain }}"
