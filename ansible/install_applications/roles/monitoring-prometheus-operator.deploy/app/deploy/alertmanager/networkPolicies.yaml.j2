kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: allow-to-alertmanager-main
  namespace: {{ prometheus.namespace }}
  labels:
    app: alertmanager
    alertmanager: main
spec:
  types: [Ingress]

  podSelector:
    matchLabels:
      app: alertmanager
      alertmanager: main

  ingress:
    # Allow from the ingress controller
    - ports:
        - port: web
      from:
        - namespaceSelector:
            matchLabels:
              app.kubernetes.io/name: ingress-nginx
          podSelector:
            matchLabels:
              app.kubernetes.io/name: ingress-nginx
