apiVersion: v1
kind: Service
metadata:
  name: kibana
  namespace: {{ kibana.namespace }}
  labels:
    app.kubernetes.io/name: kibana
spec:
  ports:
  - port: 5601
  selector:
    app.kubernetes.io/name: kibana
    app.kubernetes.io/component: kibana
