apiVersion: v1
kind: Service
metadata:
  name: kibana
  namespace: {{ kibana.namespace }}
  labels:
    app: kibana
spec:
  ports:
  - port: 5601
  selector:
    app: kibana
    app-component: kibana
