apiVersion: v1
kind: Service
metadata:
  name: openldap
  namespace: {{ openldap.namespace }}
  labels:
    app.kubernetes.io/name: openldap
spec:
  ports:
  - port: 389
    name: ldap
  - port: 636
    name: ldaps
  selector:
    app.kubernetes.io/name: openldap
    app.kubernetes.io/component: openldap
