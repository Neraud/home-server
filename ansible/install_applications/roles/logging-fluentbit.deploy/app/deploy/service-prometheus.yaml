apiVersion: v1
kind: Service
metadata:
  name: fluent-bit-prometheus
  namespace: logging
  labels:
    app: fluent-bit
    app-component: fluent-bit
    prometheus-target: fluent-bit
spec:
  ports:
  - port: 2020
    name: metrics
    targetPort: metrics
  selector:
    app: fluent-bit
    app-component: fluent-bit
