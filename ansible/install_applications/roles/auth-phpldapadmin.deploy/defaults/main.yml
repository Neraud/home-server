---

phpldapadmin_enabled: true

phpldapadmin_ldap_servername: "{{ openldap.ldap.servername }}"
phpldapadmin_bind_id: "{{ openldap.ldap.readonly_user_dn }}"
phpldapadmin_bind_password: "{{ openldap.ldap.readonly_user_password }}"
