apiVersion: v1
kind: Service
metadata:
  name: lemonldap
  namespace: {{ lemonldap.namespace }}
  labels:
    app: lemonldap
spec:
  ports:
  - port: 80
    name: http
  selector:
    app: lemonldap
    app-component: lemonldap
