apiVersion: v1
kind: Service
metadata:
  name: lemonldap
  namespace: {{ lemonldap.namespace }}
  labels:
    app.kubernetes.io/name: lemonldap
spec:
  ports:
  - port: 80
    name: http
  selector:
    app.kubernetes.io/name: lemonldap
    app.kubernetes.io/component: lemonldap
