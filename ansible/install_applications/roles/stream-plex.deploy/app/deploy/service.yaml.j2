apiVersion: v1
kind: Service
metadata:
  name: plex
  namespace: {{ plex.namespace }}
  labels:
    app: plex
    app-component: plex
spec:
  ports:
  - port: 80
    targetPort: 32400
  selector:
    app: plex
    app-component: plex
