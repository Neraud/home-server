server:
  listenaddr: "" # the address to bind on, leave empty to bind on all addresses
  # Must be set as an env var, as the DockerFiel already defines a default value
  #port: 8080 # the port for the http server
  ssl:
    enabled: false # if https should be enabled
database: # see below
  dialect: sqlite3
  connection: data/db/gotify.db
defaultuser: # on database creation, gotify creates an admin user (these values will only be used for the first start, if you want to edit the user after the first start use the WebUI)
  name: {{ gotify.default_admin_user }} # the username of the default user
  pass: {{ gotify.default_admin_password }} # the password of the default user
passstrength: 10 # the bcrypt password strength (higher = better but also slower)
uploadedimagesdir: data/images # the directory for storing uploaded images
pluginsdir: data/plugins # the directory where plugin resides (leave empty to disable plugins)
