kind: Service
apiVersion: v1
metadata:
  name: gotify
  namespace: {{ gotify.namespace }}
  labels:
    app.kubernetes.io/name: gotify
    app.kubernetes.io/component: gotify
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
  selector:
    app.kubernetes.io/name: gotify
    app.kubernetes.io/component: gotify
