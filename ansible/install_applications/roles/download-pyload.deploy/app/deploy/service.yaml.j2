apiVersion: v1
kind: Service
metadata:
  name: pyload
  namespace: {{ pyload.namespace }}
  labels:
    app.kubernetes.io/name: pyload
spec:
  ports:
  - port: 80
    targetPort: 8000
    name: webui
  selector:
    app.kubernetes.io/name: pyload
    app.kubernetes.io/component: pyload
