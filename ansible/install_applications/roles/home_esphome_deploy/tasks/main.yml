---
- name: Ensure ESPHome namespace exists
  ansible.builtin.import_tasks: ../../app_base_deploy/tasks/custom-namespace.yml
  vars:
    application_namespace: "{{ esphome.namespace }}"

- name: Deploy ESPHome backup script
  ansible.builtin.import_tasks: ../../app_base_deploy/tasks/backup.yml
  vars:
    app_backup: "{{ esphome.app_backup }}"
  tags: backup

- name: Ensure BusyBox image is up-to-date in the private registry
  ansible.builtin.import_tasks: ../../app_base_deploy/tasks/docker-image-private-registry.yml
  vars:
    base_image: "{{ busybox.base_image }}"
    private_image: "{{ busybox.container.image }}"

- name: Ensure ESPHome image is up-to-date in the private registry
  ansible.builtin.import_tasks: ../../app_base_deploy/tasks/docker-image-private-registry.yml
  vars:
    base_image: "{{ esphome.base_image }}"
    private_image: "{{ esphome.container.image }}"
    build: yes

- name: Deploy ESPHome
  kubernetes.core.k8s:
    definition: "{{ lookup(item | regex_search('.j2$') | ternary('template','file'), item) }}"
    state: present
    kubeconfig: "{{ kubeconfig_path }}"
    context: "{{ kubeconfig_context }}"
  with_fileglob:
    - "{{ role_path }}/app/deploy/*.yaml*"
