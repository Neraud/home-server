apiVersion: v1
kind: Service
metadata:
  name: heimdall
  namespace: default
  labels:
    app: heimdall
    app-component: heimdall
spec:
  ports:
  - port: 80
  selector:
    app: heimdall
    app-component: heimdall
