apiVersion: v1
kind: Service
metadata:
  name: gitlab
  namespace: default
  labels:
    app: gitlab
    app-component: gitlab
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: gitlab
    app-component: gitlab
