
apiVersion: v1
kind: Service
metadata:
  name: fluentd
  namespace: logging
  labels:
    app: fluentd
spec:
  clusterIP: None
  ports:
    - name: fluentd
      port: 24224
      targetPort: 24224
  selector:
    app: fluentd
    app-component: fluentd
