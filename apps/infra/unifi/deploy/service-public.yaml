
apiVersion: v1
kind: Service
metadata:
  name: unifi-frontend
  namespace: default
  labels:
    app: unifi
    tier: frontend
spec:
  ports:
  # Port used for controller GUI/API as seen in a web browser
  - port: 443
    targetPort: 8443
  selector:
    app: unifi
    tier: frontend
