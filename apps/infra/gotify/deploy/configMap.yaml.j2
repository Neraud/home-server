apiVersion: v1
kind: ConfigMap
metadata:
  name: gotify-config
  namespace: default
  labels:
    app: gotify
    app-component: gotify
data:
  config.yml: |
    {{ lookup('template', kubernetes_app_root_path + '/infra/gotify/config/config.yml.j2')  | replace('\n', '\n    ') }}
