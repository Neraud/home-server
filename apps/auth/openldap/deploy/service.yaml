apiVersion: v1
kind: Service
metadata:
  name: openldap
  namespace: default
  labels:
    app: openldap
spec:
  ports:
  - port: 389
    name: ldap
  selector:
    app: openldap
    app-component: openldap
