apiVersion: v1
kind: Service
metadata:
  name: openldap
  namespace: default
  labels:
    app: openldap
spec:
  ports:
  - port: 636
    name: ldaps
  selector:
    app: openldap
    app-component: openldap
