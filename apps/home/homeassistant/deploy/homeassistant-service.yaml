apiVersion: v1
kind: Service
metadata:
  name: homeassistant
  namespace: default
  labels:
    app: homeassistant
    tier: frontend
spec:
  ports:
  - port: 80
    targetPort: 8123
  selector:
    app: homeassistant
    tier: frontend
