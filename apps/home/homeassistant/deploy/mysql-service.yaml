apiVersion: v1
kind: Service
metadata:
  name: homeassistant-mysql
  namespace: default
  labels:
    app: homeassistant
    tier: database
spec:
  ports:
    - port: 3306
  selector:
    app: homeassistant
    tier: database
  clusterIP: None
