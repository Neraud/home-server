apiVersion: v1
kind: Service
metadata:
  name: mosquitto
  namespace: default
  labels:
    app: mosquitto
spec:
  ports:
  - port: 1883
    name: mqtt
  - port: 8883
    name: mqtts
  selector:
    app: mosquitto
    app-component: mosquitto
