apiVersion: v1
kind: Service
metadata:
  name: mosquitto
  namespace: default
  labels:
    app: mosquitto
    tier: backend
spec:
  ports:
  - port: 1883
  selector:
    app: mosquitto
    tier: backend
