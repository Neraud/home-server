apiVersion: v1
kind: Service
metadata:
  name: ttrss-pgsql
  namespace: default
  labels:
    app: ttrss
    tier: database
spec:
  ports:
  - port: 5432
  selector:
    app: ttrss
    tier: database
  clusterIP: None
