apiVersion: v1
kind: Service
metadata:
  name: sickchill
  namespace: default
  labels:
    app: sickchill
spec:
  ports:
  - port: 80
    targetPort: 8081
    name: web
  selector:
    app: sickchill
    app-component: sickchill
