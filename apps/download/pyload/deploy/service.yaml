apiVersion: v1
kind: Service
metadata:
  name: pyload
  namespace: default
  labels:
    app: pyload
spec:
  ports:
  - port: 80
    targetPort: 8000
    name: webui
  selector:
    app: pyload
    app-component: pyload
