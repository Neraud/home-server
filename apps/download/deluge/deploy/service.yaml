apiVersion: v1
kind: Service
metadata:
  name: deluge
  namespace: default
  labels:
    app: deluge
spec:
  ports:
  - port: 80
    targetPort: 8112
    name: webui
  selector:
    app: deluge
    app-component: deluge
