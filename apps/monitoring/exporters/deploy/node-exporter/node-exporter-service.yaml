apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: node-exporter
    prometheus-target: node-exporter 
  name: node-exporter
  namespace: monitoring
spec:
  clusterIP: None
  ports:
  - name: https
    port: 9100
    targetPort: https
  selector:
    app: node-exporter
