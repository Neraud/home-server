apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pyload-data
spec:
  accessModes: [ "ReadWriteOnce" ]
  storageClassName: longhorn-high-durability
  resources:
    requests:
      storage: "32Mi"
