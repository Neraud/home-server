apiVersion: v1
kind: Secret
metadata:
  name: argocd-ci-token
  annotations:
    reflector.v1.k8s.emberstack.com/reflection-allowed: "true"
    reflector.v1.k8s.emberstack.com/reflection-allowed-namespaces: "dev-argo-workflows-tasks"
type: Opaque

# The token is created using the argocd cli and is injected here by ansible
#data:
#  ARGOCD_AUTH_TOKEN: xxx
