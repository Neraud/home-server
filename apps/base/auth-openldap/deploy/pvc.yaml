apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openldap-data
spec:
  accessModes: [ "ReadWriteOnce" ]
  storageClassName: longhorn-high-durability
  resources:
    requests:
      storage: "256Mi"
