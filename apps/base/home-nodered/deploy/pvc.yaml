apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nodered-data
spec:
  accessModes: [ "ReadWriteOnce" ]
  storageClassName: longhorn-high-durability
  resources:
    requests:
      storage: "128Mi"
