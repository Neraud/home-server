watchers:
  - type: docker_registry
    repo: registry-1.docker.io
    image: library/registry
    tag: 3.1.1
    includes:
      - "[0-9]+\\.[0-9]+\\.[0-9]+.*"
    excludes:
      - latest
      - .*(alpha|beta|rc).*
      - ^[0-2]\.
      - ^3\.0\.
