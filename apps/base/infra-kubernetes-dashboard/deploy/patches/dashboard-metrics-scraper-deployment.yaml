kind: Deployment
apiVersion: apps/v1
metadata:
  name: kubernetes-dashboard-metrics-scraper
spec:
  template:
    spec:
      containers:
        - name: kubernetes-dashboard-metrics-scraper
      imagePullSecrets:
      - name: docker-config
      nodeSelector:
        capability/general-purpose: 'yes'