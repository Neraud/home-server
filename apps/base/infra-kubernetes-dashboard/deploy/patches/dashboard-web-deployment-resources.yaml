kind: Deployment
apiVersion: apps/v1
metadata:
  name: kubernetes-dashboard-web
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: kubernetes-dashboard-web
          resources:
            limits:
              cpu: 250m
              memory: 64Mi
            requests:
              cpu: 25m
              memory: 64Mi
