apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes-dashboard-kong
  namespace:  kubernetes-dashboard
spec:
  template:
    spec:
      containers:
      - name: "proxy"
      imagePullSecrets:
      - name: docker-config
      nodeSelector:
        capability/general-purpose: 'yes'