kind: Deployment
apiVersion: apps/v1
metadata:
  name: kubernetes-dashboard-web
spec:
  template:
    spec:
      containers:
        - name: kubernetes-dashboard-web
      imagePullSecrets:
      - name: docker-config
      nodeSelector:
        capability/general-purpose: 'yes'