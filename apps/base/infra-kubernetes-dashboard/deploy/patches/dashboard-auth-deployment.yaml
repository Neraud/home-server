kind: Deployment
apiVersion: apps/v1
metadata:
  name: kubernetes-dashboard-auth
spec:
  template:
    spec:
      containers:
        - name: kubernetes-dashboard-auth
      imagePullSecrets:
      - name: docker-config
      nodeSelector:
        capability/general-purpose: 'yes'