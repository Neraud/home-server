apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperless-data
spec:
  accessModes: [ "ReadWriteOnce" ]
  storageClassName: longhorn-high-durability
  resources:
    requests:
      storage: 128Mi
