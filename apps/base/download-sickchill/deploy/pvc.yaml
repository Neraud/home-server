apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sickchill-data
spec:
  accessModes: [ "ReadWriteOnce" ]
  storageClassName: longhorn-high-durability
  resources:
    requests:
      storage: "1Gi"
