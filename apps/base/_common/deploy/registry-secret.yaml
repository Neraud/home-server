apiVersion: v1
kind: Secret
type: kubernetes.io/dockerconfigjson
metadata:
  name: docker-config
  annotations:
    reflector.v1.k8s.emberstack.com/reflects: "cluster-secrets/docker-config"
data:
  .dockerconfigjson: e30K  # this is `{}` base64 encoded
