apiVersion: apps/v1
kind: Deployment
metadata:
  name: longhorn-recovery-backend
  namespace: longhorn-system
spec:
  template:
    spec:
      nodeSelector:
        capability/general-purpose: 'yes'
